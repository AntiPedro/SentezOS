name: SentezOS CI/CD Pipeline

on:
  push:
      branches: [ "main" ]
        workflow_dispatch:

        jobs:
          build-and-compile:
              name: SentezOS ISO Derleme
                  runs-on: ubuntu-latest
                      container:
                            image: archlinux:latest
                                  options: --privileged

                                      steps:
                                            - name: Kodlar
